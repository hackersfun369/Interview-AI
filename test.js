const nav = document.querySelector(".nav");

/* window.addEventListener("scroll", () => {
    if (window.scrollY > 0) {
        nav.classList.add("scrolled");
        nav.backgroundColor="#2F80ED"
    } else {
        nav.classList.remove("scrolled");
    }
});
 */